{
  "name": "hungie-api",
  "build": {
    "commands": [
      "pip3 install -r requirements-prod.txt"
    ]
  },
  "start": {
    "command": "python3 prod_server.py"
  },
  "env": {
    "ENVIRONMENT": "production",
    "PORT": "$PORT"
  }
}
